{
  "installCommand": "echo 'skip root install'",
  "buildCommand": "cd packages/sdk && npm install && npm run build && cd ../../apps/web && npm install && npm run build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
