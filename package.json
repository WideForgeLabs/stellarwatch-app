{
  "name": "stellarwatch-app-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for StellarWatch app: SDK, web dashboard, and indexer",
  "workspaces": [
    "packages/*",
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@stellarwatch/sdk && npm run build --workspace=@stellarwatch/web",
    "build:sdk": "npm run build --workspace=@stellarwatch/sdk",
    "build:web": "npm run build --workspace=@stellarwatch/web",
    "dev": "npm run dev --workspace=@stellarwatch/web"
  },
  "license": "MIT"
}
